# OpenRCA SigNoz 数据导入工具

将 OpenRCA 数据集的 metric 和 trace 数据导入到 SigNoz 进行可视化分析。支持将历史数据的时间戳映射到当前时间15天内，方便在 SigNoz 界面查看。

## 功能特性

- 📊 读取 OpenRCA 数据集中的 metric 和 trace 数据文件
  - 支持 `metric_app.csv`（应用指标）
  - 支持 `metric_container.csv`（容器指标）
  - 支持 `trace_span.csv`（追踪数据）
- ⏰ 支持时间戳映射：将历史日期映射到当前时间15天内
- 🚀 通过 OpenTelemetry Protocol (OTLP) 将数据发送到 SigNoz
- 📦 支持批量处理以提高性能
- 💾 流式处理，适合处理大文件
- 🔧 自动构建数据目录路径（基于源日期）
- 🔄 导入顺序：先导入 metric，再导入 trace

## 前置要求

1. **Python 3.8+**
2. **已部署的 SigNoz 实例**（本地 Docker 部署）
3. **OpenRCA 数据集**（位于 `datasets/OpenRCA/Bank/telemetry/` 目录）

## 安装

```bash
pip install -r requirements.txt
```

## 快速开始

### 1. 确保 SigNoz 正在运行

确保你的 SigNoz 实例正在运行，并且 OTLP gRPC 端点可访问（默认是 `localhost:4317`）。

```bash
# 检查 SigNoz 是否在运行
docker ps | grep signoz

# 或者检查端口是否开放
nc -zv localhost 4317
```

### 2. 运行导入脚本

**基本用法（最简单）：**

```bash
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19
```

脚本会自动根据 `--source-date` 构建数据目录路径：`datasets/OpenRCA/Bank/telemetry/2021_03_04`

**完整示例（自定义所有参数）：**

```bash
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19 \
  --signoz-endpoint localhost:4317 \
  --data-dir datasets/OpenRCA/Bank/telemetry/2021_03_04 \
  --service-name openrca-bank \
  --batch-size 1000
```

### 3. 查看结果

1. 打开 SigNoz Web 界面（通常是 `http://localhost:3301`）
2. 导航到相应的页面：
   - **Metrics 页面**：查看导入的 metric 数据（应用指标和容器指标）
   - **Traces 页面**：查看导入的 trace 数据
3. 选择时间范围（使用目标日期附近的时间范围，例如：2024-12-19 前后）
4. 查看导入的数据

## 参数说明

### 必需参数

- `--source-date YYYY-MM-DD`: 源日期，datasets 中的历史日期（例如: `2021-03-04`）
  - 用于时间映射计算
  - 如果不提供 `--data-dir`，会根据此参数自动构建数据目录路径（格式：`datasets/OpenRCA/Bank/telemetry/YYYY_MM_DD`）

- `--target-date YYYY-MM-DD`: 目标日期，映射到的日期（必须在当前时间15天内，例如: `2024-12-19`）
  - 用于时间映射计算
  - 必须不晚于当前时间
  - 必须在当前时间15天内

### 可选参数

- `--data-dir`: 数据目录路径（可选）
  - 如果不提供，会根据 `--source-date` 自动构建路径
  - 如果提供，使用指定的路径（例如: `datasets/OpenRCA/Bank/telemetry/2021_03_04`）
  - 仅用于定位数据加载路径，不影响时间映射逻辑

- `--signoz-endpoint`: SigNoz OTLP gRPC 端点（默认: `localhost:4317`）

- `--service-name`: 服务名称（默认: `openrca-bank`）

- `--batch-size`: 批处理大小（默认: `1000`）

- `--unified-container-metric`: 使用统一的container metric名称（默认: True）
  - 将所有container metric统一为一个metric（`container_metric`），使用 `kpi_name` 标签区分
  - 这样可以大大减少metric数量（从349个减少到1个），便于在SigNoz中管理和查询
  - 推荐使用此模式

- `--no-unified-container-metric`: 不使用统一的container metric名称
  - 每个kpi_name创建一个独立的metric（会产生349个metric）
  - 仅在需要独立metric名称时使用

- `--group-container-by-category`: 按大类分组container metric（默认: True，需要--unified-container-metric）
  - 将349个metric减少到6个（每个大类一个）
  - 创建的metric：
    - `container_mysql_metric` (MySQL相关，124个KPI)
    - `container_oslinux_metric` (操作系统Linux相关，123个KPI)
    - `container_redis_metric` (Redis相关，34个KPI)
    - `container_container_metric` (Docker容器相关，30个KPI)
    - `container_tomcat_metric` (Tomcat相关，24个KPI)
    - `container_jvm_metric` (JVM相关，14个KPI)

- `--no-group-container-by-category`: 不使用大类分组container metric（默认: 开启）
  - 使用统一的container_metric（所有349个KPI在一个metric中）

## Container Metric 优化说明

### 问题
3月4日的container metric数据包含**349个不同的kpi_name**，如果每个kpi_name都创建一个独立的metric，会在SigNoz中产生349个metric，难以管理和查询。

### 解决方案：统一Metric模式（推荐）

**默认启用**：使用 `--unified-container-metric`（默认）或 `--no-unified-container-metric` 禁用

**工作原理**：
- 所有container metric统一为一个metric名称：`container_metric`
- 使用 `kpi_name` 标签区分不同的指标
- 使用 `cmdb_id` 标签区分不同的容器

**优势**：
- ✅ Metric数量从349个减少到1个
- ✅ 在SigNoz中更容易管理和查询
- ✅ 可以使用标签过滤查看特定指标
- ✅ 减少存储和查询开销

**在SigNoz中查询统一后的Container Metric**：

1. **选择metric**: `container_metric`

2. **使用标签过滤**：
   - 查看特定KPI：`kpi_name = OSLinux-CPU_CPU_CPUCpuUtil`
   - 查看特定容器：`cmdb_id = Tomcat04`
   - 组合查询：`cmdb_id = Tomcat04 AND kpi_name = OSLinux-CPU_CPU_CPUCpuUtil`

3. **PromQL查询示例**：
   ```promql
   # 查看特定KPI的所有容器
   container_metric{kpi_name="OSLinux-CPU_CPU_CPUCpuUtil"}
   
   # 查看特定容器的所有KPI
   container_metric{cmdb_id="Tomcat04"}
   
   # 查看特定容器的特定KPI
   container_metric{cmdb_id="Tomcat04", kpi_name="OSLinux-CPU_CPU_CPUCpuUtil"}
   
   # 按kpi_name分组查看平均值
   avg(container_metric) by (kpi_name)
   
   # 按cmdb_id分组查看平均值
   avg(container_metric) by (cmdb_id)
   ```

4. **在SigNoz UI中使用**：
   - 进入 Metrics Explorer
   - 选择指标：`container_metric`
   - 添加过滤器：
     - Label: `kpi_name`, Operator: `=`, Value: `OSLinux-CPU_CPU_CPUCpuUtil`
     - Label: `cmdb_id`, Operator: `=`, Value: `Tomcat04`
   - 选择聚合函数：`avg`, `min`, `max`, `sum` 等
   - 选择图表类型：Line Chart

**使用示例**：
```bash
# 使用按大类分组模式（默认，推荐，将349个metric减少到6个）
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19

# 或者显式指定
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19 \
  --unified-container-metric \
  --group-container-by-category

# 不使用大类分组，使用统一的container_metric（所有349个KPI在一个metric中）
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19 \
  --unified-container-metric \
  --no-group-container-by-category

# 不使用统一metric模式（会产生349个metric）
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19 \
  --no-unified-container-metric
```

### 方案二：按大类分组（默认，推荐）

**默认启用**：默认开启大类分组模式（需要同时使用 `--unified-container-metric`，默认已开启）

**工作原理**：
- 根据kpi_name的前缀（第一个'-'之前的部分）自动识别大类
- 将349个metric按大类分组为6个metric：
  - `container_mysql_metric` - MySQL数据库相关指标（124个KPI）
  - `container_oslinux_metric` - 操作系统Linux相关指标（123个KPI）
  - `container_redis_metric` - Redis相关指标（34个KPI）
  - `container_container_metric` - Docker容器相关指标（30个KPI）
  - `container_tomcat_metric` - Tomcat应用服务器相关指标（24个KPI）
  - `container_jvm_metric` - Java虚拟机相关指标（14个KPI）

**优势**：
- ✅ Metric数量从349个减少到6个
- ✅ 按大类组织，更易于管理和查询
- ✅ 可以使用标签过滤查看特定指标
- ✅ 减少存储和查询开销

**在SigNoz中查询按大类分组的Container Metric**：

1. **选择metric**: 根据大类选择对应的metric，例如：
   - `container_mysql_metric` - 查看MySQL相关指标
   - `container_oslinux_metric` - 查看操作系统相关指标
   - `container_redis_metric` - 查看Redis相关指标
   - 等等

2. **使用标签过滤**：
   - 查看特定KPI：`kpi_name = OSLinux-CPU_CPU_CPUCpuUtil`
   - 查看特定容器：`cmdb_id = Tomcat04`
   - 组合查询：`cmdb_id = Tomcat04 AND kpi_name = OSLinux-CPU_CPU_CPUCpuUtil`

3. **PromQL查询示例**：
   ```promql
   # 查看MySQL相关的所有指标
   container_mysql_metric
   
   # 查看特定KPI的所有容器
   container_oslinux_metric{kpi_name="OSLinux-CPU_CPU_CPUCpuUtil"}
   
   # 查看特定容器的所有KPI
   container_mysql_metric{cmdb_id="Mysql02"}
   
   # 按kpi_name分组查看平均值
   avg(container_mysql_metric) by (kpi_name)
   
   # 按cmdb_id分组查看平均值
   avg(container_oslinux_metric) by (cmdb_id)
   ```

4. **在SigNoz UI中使用**：
   - 进入 Metrics Explorer
   - 选择指标：例如 `container_mysql_metric`
   - 添加过滤器：
     - Label: `kpi_name`, Operator: `=`, Value: `Mysql-MySQL_3306_Connections`
     - Label: `cmdb_id`, Operator: `=`, Value: `Mysql02`
   - 选择聚合函数：`avg`, `min`, `max`, `sum` 等
   - 选择图表类型：Line Chart

## 时间戳映射说明

脚本会将源日期的数据时间戳映射到目标日期，这样可以在 SigNoz 的最近15天内查看历史数据。

**工作原理：**
- **源日期**（`--source-date`）：指定 datasets 中的历史日期（例如: `2021-03-04`）
- **目标日期**（`--target-date`）：将历史数据映射到的日期（必须在当前时间15天内，例如: `2024-12-19`）
- **时间偏移**：脚本会自动计算两个日期之间的时间差，并将所有时间戳进行相应调整

**示例：**
```bash
# 将 2021-03-04 的数据映射到 2024-12-19
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19
```

这样，2021-03-04 的数据在 SigNoz 中会显示为 2024-12-19，方便在最近15天内查看。

## 数据格式

脚本会读取以下格式的 CSV 文件：

### Metric 文件

**metric_app.csv**（应用指标）：
```csv
timestamp,rr,sr,cnt,mrt,tc
1614787440,100.0,100.0,22,53.27,ServiceTest1
```
- `timestamp`: 时间戳（秒）
- `rr`: 请求率 (Request Rate)
- `sr`: 成功率 (Success Rate)
- `cnt`: 计数 (Count)
- `mrt`: 平均响应时间 (Mean Response Time)
- `tc`: 服务名称 (Service Name)

**metric_container.csv**（容器指标）：
```csv
timestamp,cmdb_id,kpi_name,value
1614787200,Tomcat04,OSLinux-CPU_CPU_CPUCpuUtil,26.2957
```
- `timestamp`: 时间戳（秒）
- `cmdb_id`: 组件ID
- `kpi_name`: KPI指标名称
- `value`: 指标值

### Trace 文件

**trace_span.csv**（追踪数据）：
```csv
timestamp,cmdb_id,parent_id,span_id,trace_id,duration
1614787199628,dockerA2,369-bcou-dle-way1-c514cf30-43410@0824-2f0e47a816-17492,21030300016145905763,gw0120210304000517192504,19
```
- `timestamp`: 时间戳（毫秒）
- `cmdb_id`: 组件/服务标识
- `parent_id`: 父 span ID
- `span_id`: 当前 span ID
- `trace_id`: trace ID
- `duration`: 持续时间（毫秒）

## 使用示例

### 示例1：使用自动构建的数据目录（推荐）

```bash
# 脚本会自动构建路径：datasets/OpenRCA/Bank/telemetry/2021_03_04
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19
```

### 示例2：指定自定义数据目录

```bash
# 使用自定义的数据目录路径
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19 \
  --data-dir /path/to/custom/data/dir
```

### 示例3：自定义 SigNoz 端点

```bash
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19 \
  --signoz-endpoint your-signoz-host:4317
```

### 示例4：调整批处理大小

```bash
# 对于大数据集，可以增加批处理大小以提高性能
python main.py \
  --source-date 2021-03-04 \
  --target-date 2024-12-19 \
  --batch-size 2000
```

## 常见问题

### Q: 数据没有显示在 SigNoz 中？

**A:** 
- 等待几分钟让数据索引完成
- 检查时间范围设置（使用目标日期附近的时间范围）
- 查看 SigNoz 日志确认数据是否成功接收：
  ```bash
  docker logs <signoz-otel-collector-container> --tail 50
  ```
- 确保服务名称过滤器选择了正确的服务（或清除所有过滤器）
- 对于 metric 数据，检查 Metrics 页面；对于 trace 数据，检查 Traces 页面

### Q: 连接错误？

**A:**
- 确认 SigNoz 正在运行：`docker ps | grep signoz`
- 检查 OTLP 端点是否正确（默认: `localhost:4317`）
- 如果使用 Docker，确保端口映射正确
- 检查防火墙设置是否允许连接

### Q: 目标日期验证失败？

**A:**
- 目标日期必须在当前时间15天内
- 目标日期不能晚于当前时间
- 检查日期格式是否正确：`YYYY-MM-DD`

### Q: 数据目录不存在？

**A:**
- 如果不提供 `--data-dir`，脚本会根据 `--source-date` 自动构建路径
- 确保 `--source-date` 格式正确（例如: `2021-03-04` 对应文件夹 `2021_03_04`）
- 如果使用 `--data-dir`，确保指定的路径存在且包含以下文件：
  - `metric/metric_app.csv`（可选）
  - `metric/metric_container.csv`（可选）
  - `trace/trace_span.csv`（可选）

### Q: 处理速度慢？

**A:**
- 数据集可能很大，这是正常的
- 可以调整 `--batch-size` 参数来优化性能（默认: 1000）
- 对于非常大的数据集，考虑分批处理不同日期的数据

## 注意事项

1. **导入顺序**: 脚本会先导入 metric 数据（metric_app.csv 和 metric_container.csv），然后导入 trace 数据（trace_span.csv）
2. **时间戳映射**: 脚本会将源日期的数据时间戳映射到目标日期，确保数据在 SigNoz 中显示为目标日期的时间
3. **15天限制**: 目标日期必须在当前时间15天内，这是为了符合 SigNoz 的数据保留策略
4. **自动路径构建**: 如果不提供 `--data-dir`，脚本会根据 `--source-date` 自动构建数据目录路径（格式：`datasets/OpenRCA/Bank/telemetry/YYYY_MM_DD`）
5. **流式处理**: 脚本采用流式处理方式，边读边发送，不会一次性加载所有数据到内存，适合处理大文件
6. **网络连接**: 确保能够访问 SigNoz 的 OTLP 端点
7. **处理时间**: 如果数据集很大，处理可能需要一些时间，请耐心等待
8. **Metric 时间戳**: Metric 文件使用秒级时间戳，Trace 文件使用毫秒级时间戳，脚本会自动处理

## 下一步

导入完成后，你可以在 SigNoz 中：
- 📊 查看 metric 数据（应用指标和容器指标）
- 🔍 查看 trace 的完整调用链
- 🔗 分析服务之间的依赖关系
- ⚡ 识别性能瓶颈
- 🐛 查看错误和异常
- 📈 进行性能分析和优化
- 📉 监控系统指标和资源使用情况

## 创建 Metric 可视化图表

在 SigNoz 中创建 metric 可视化图表的步骤：

### 1. 访问 Metrics 页面

1. 打开 SigNoz Web 界面（通常是 `http://localhost:3301`）
2. 在左侧导航栏中，点击 **"Metrics"** 或 **"指标"** 菜单

### 2. 查看可用的 Metrics

在 Metrics 页面，你可以看到所有已导入的指标。根据你的数据，应该能看到：

**应用指标（metric_app.csv）：**
- `app_request_rate` - 应用请求率
- `app_success_rate` - 应用成功率
- `app_request_count` - 应用请求计数
- `app_response_time` - 应用响应时间（直方图）

**容器指标（metric_container.csv）：**
- `container_*` - 各种容器KPI指标（根据kpi_name动态创建）

### 3. 创建可视化图表

#### 方法一：使用 Metrics Explorer（推荐）

1. **选择指标**
   - 在 Metrics Explorer 中，点击 **"Select Metric"** 或 **"选择指标"**
   - 从下拉列表中选择你要查看的指标（例如：`app_request_rate`）

2. **设置时间范围**
   - 点击右上角的时间选择器
   - 选择 **"Custom Time Range"** 或使用预设范围
   - 设置时间范围为你的目标日期附近（例如：如果 `--target-date` 是 `2024-12-19`，选择 `2024-12-19` 前后几天）
   - **重要**：时区选择 **UTC**

3. **添加过滤器（使用 Attributes/标签）**
   - 在 SigNoz 中，代码中设置的 `attributes` 会显示为**标签（Labels/Tags）**
   - 可以点击 **"Add Filter"** 或 **"添加过滤器"** 来过滤数据
   - **重要**：在代码中设置的 attributes 名称会直接作为标签名使用
   
   **对于 app_request_rate 等应用指标：**
   - 标签名：`service.name`
   - 示例：`service.name = ServiceTest1` 来查看特定服务的指标
   - 示例：`service.name =~ /ServiceTest.*/` 来查看所有ServiceTest开头的服务（如果支持正则）
   
   **对于 container_* 容器指标：**
   - 标签名：`cmdb_id` 和 `kpi_name`
   - 示例：`cmdb_id = Tomcat04` 来查看特定容器的指标
   - 示例：`kpi_name = OSLinux-CPU_CPU_CPUCpuUtil` 来查看特定KPI

4. **选择聚合方式**
   - 选择聚合函数：`sum`、`avg`、`min`、`max`、`rate` 等
   - 对于 Counter 类型，通常使用 `rate` 或 `sum`
   - 对于 Gauge 类型，通常使用 `avg`、`min`、`max`

5. **选择图表类型**
   - **Line Chart（折线图）**：适合查看时间序列趋势
   - **Area Chart（面积图）**：适合查看累积值
   - **Bar Chart（柱状图）**：适合对比不同标签的值

6. **保存图表（可选）**
   - 点击 **"Save"** 或 **"保存"** 按钮
   - 给图表命名，例如："应用请求率趋势"
   - 保存后可以在 Dashboard 中使用

#### 方法二：创建 Dashboard

1. **创建新的 Dashboard**
   - 在左侧导航栏中，点击 **"Dashboards"** 或 **"仪表板"**
   - 点击 **"New Dashboard"** 或 **"新建仪表板"**
   - 输入仪表板名称，例如："OpenRCA 应用监控"

2. **添加图表面板**
   - 点击 **"Add Panel"** 或 **"添加面板"**
   - 选择 **"Metrics"** 作为数据源
   - 按照上述步骤配置指标和可视化选项

3. **添加多个面板**
   - 可以添加多个面板来展示不同的指标
   - 建议创建以下面板：
     - **请求率趋势**：`app_request_rate` (Line Chart)
     - **成功率监控**：`app_success_rate` (Line Chart)
     - **响应时间分布**：`app_response_time` (Histogram/Area Chart)
     - **请求计数**：`app_request_count` (Line Chart)
     - **容器指标概览**：选择重要的 `container_*` 指标

4. **调整布局**
   - 拖拽面板调整位置和大小
   - 可以设置面板标题和描述

5. **保存 Dashboard**
   - 点击 **"Save"** 保存仪表板
   - 之后可以随时查看和编辑

### 4. 常用查询示例

#### 查看特定服务的请求率
```
Metric: app_request_rate
Filter: service.name = ServiceTest1
Aggregation: rate
Chart Type: Line Chart
```

#### 查看所有服务的请求率对比（多服务在同一图表）
这是最常用的场景，展示多个服务的请求率对比：

**步骤：**
1. Metric: `app_request_rate`
2. **Group By**: 选择 `service.name` ⭐（关键步骤）
3. Aggregation: `rate` 或 `sum`
4. Chart Type: `Line Chart`
5. 时间范围：选择目标日期附近，时区 UTC

**效果：**
- 每个服务会显示为不同颜色的折线
- 可以在图例中点击服务名来显示/隐藏特定服务
- 方便对比不同服务的请求率趋势

**示例配置：**
```
Metric: app_request_rate
Group By: service.name
Aggregation: rate
Chart Type: Line Chart
Time Range: 2024-12-19 前后几天 (UTC)
```

#### 查看所有服务的成功率对比
```
Metric: app_success_rate
Group By: service.name  ⭐（关键：按服务分组）
Aggregation: avg
Chart Type: Line Chart
```

#### 查看特定几个服务的对比（使用过滤器）
如果想只看特定几个服务，可以使用正则表达式或多个查询：

**方法1：使用正则表达式过滤器（如果支持）**
```
Metric: app_request_rate
Filter: service.name =~ /ServiceTest(1|2|3)/
Group By: service.name
Aggregation: rate
```

**方法2：创建多个查询**
- Query A: `app_request_rate` + Filter: `service.name = ServiceTest1`
- Query B: `app_request_rate` + Filter: `service.name = ServiceTest2`
- Query C: `app_request_rate` + Filter: `service.name = ServiceTest3`
- 在同一图表中显示所有查询

#### 查看响应时间分布
```
Metric: app_response_time
Aggregation: histogram
Chart Type: Histogram/Area Chart
```

#### 查看容器CPU使用率
```
Metric: container_oslinux_cpu_cpu_cpucpuutil
Filter: cmdb_id = Tomcat04
Aggregation: avg
Chart Type: Line Chart
```

### 5. 提示和技巧

1. **时间范围设置**
   - 确保时间范围包含你的目标日期
   - 使用 UTC 时区以避免时间偏移问题

2. **标签过滤（Attributes查询）**
   
   **如何找到可用的标签：**
   - 在 Metrics Explorer 中，选择指标后，通常会显示可用的标签列表
   - 或者点击 **"Show Labels"** 或 **"显示标签"** 查看所有可用的标签
   - 标签名称就是代码中设置的 attributes 的键名
   
   **应用指标的标签：**
   - `service.name` - 服务名称（来自 metric_app.csv 的 `tc` 字段）
   - 示例查询：
     - `service.name = ServiceTest1` - 查看特定服务
     - `service.name != ServiceTest1` - 排除特定服务
     - `service.name =~ /ServiceTest.*/` - 使用正则表达式（如果支持）
   
   **容器指标的标签：**
   - `cmdb_id` - 组件ID（来自 metric_container.csv）
   - `kpi_name` - KPI指标名称（来自 metric_container.csv）
   - 示例查询：
     - `cmdb_id = Tomcat04` - 查看特定容器
     - `kpi_name = OSLinux-CPU_CPU_CPUCpuUtil` - 查看特定KPI
     - `cmdb_id = Tomcat04 AND kpi_name = OSLinux-CPU_CPU_CPUCpuUtil` - 组合查询
   
   **在 SigNoz 中使用标签过滤的步骤：**
   1. 选择指标（例如：`app_request_rate`）
   2. 点击 **"Add Filter"** 或 **"添加过滤器"**
   3. 选择标签名（例如：`service.name`）
   4. 选择操作符（`=`、`!=`、`=~` 等）
   5. 输入值（例如：`ServiceTest1`）
   6. 可以添加多个过滤器，使用 AND/OR 逻辑组合

3. **聚合函数选择**
   - Counter 类型（如 `request_rate`、`request_count`）：使用 `rate` 查看速率，或 `sum` 查看累计值
   - Gauge 类型（如 `success_rate`、容器指标）：使用 `avg`、`min`、`max` 查看统计值
   - Histogram 类型（如 `response_time`）：使用 `histogram` 查看分布

4. **性能优化**
   - 如果数据量很大，可以增加采样间隔
   - 使用适当的聚合粒度（例如：1分钟、5分钟）

5. **告警设置（可选）**
   - 在 Metrics 页面可以设置告警规则
   - 例如：当 `app_success_rate` 低于 95% 时发送告警
   - 当 `app_response_time` 超过 1000ms 时发送告警

### 6. 常见问题

**Q: 看不到数据？**
- 检查时间范围是否正确（使用目标日期附近）
- 检查时区是否为 UTC
- 等待几分钟让数据索引完成
- 检查过滤器是否设置正确

**Q: 图表显示为空？**
- 确认指标名称是否正确
- 检查是否有数据在选定的时间范围内
- 尝试移除过滤器查看是否有数据

**Q: 如何对比不同服务？**

**方法1：使用 Group By（推荐）**
- 在 Metrics Explorer 中，找到 **"Group By"** 或 **"分组"** 选项
- 选择 `service.name`（这是代码中设置的 attribute 名称）
- 这样所有服务会在同一图表中显示为不同颜色的线
- 可以在图例中点击服务名来显示/隐藏特定服务

**方法2：创建多个查询**
- 在同一图表中添加多个查询
- Query 1: `app_request_rate` + Filter: `service.name = ServiceTest1`
- Query 2: `app_request_rate` + Filter: `service.name = ServiceTest2`
- 以此类推

**方法3：Dashboard 多面板**
- 创建 Dashboard，添加多个面板
- 每个面板显示一个服务的指标
- 并排显示方便对比

**示例：查看所有服务的请求率对比**
```
Metric: app_request_rate
Group By: service.name  ← 关键步骤（使用attribute名称）
Aggregation: rate
Chart Type: Line Chart
```

**Q: 如何根据 attributes 查询对应的 service？**

**步骤：**
1. **找到标签名称**：在 SigNoz 中，代码设置的 `attributes` 会显示为**标签（Labels）**
   - 对于应用指标：标签名是 `service.name`
   - 对于容器指标：标签名是 `cmdb_id` 和 `kpi_name`

2. **添加过滤器**：
   - 在 Metrics Explorer 中，点击 **"Add Filter"** 或 **"添加过滤器"**
   - 选择标签：`service.name`
   - 选择操作符：`=`（等于）
   - 输入值：例如 `ServiceTest1`
   - 点击应用

3. **查看结果**：图表会只显示匹配该服务的数据

4. **查看所有可用的 service 值**：
   - 在标签选择器中，通常会显示所有可用的值
   - 或者先不设置过滤器，查看图表图例，会显示所有出现的服务名

**示例查询：**
```
Metric: app_request_rate
Filter: service.name = ServiceTest1  ← 使用attribute名称作为标签名
Aggregation: rate
Chart Type: Line Chart
```

**代码中的 attributes 映射：**
- `attributes = {"service.name": service_name}` → 在 SigNoz 中标签名为 `service.name`
- `attributes = {"cmdb_id": cmdb_id, "kpi_name": kpi_name}` → 在 SigNoz 中标签名为 `cmdb_id` 和 `kpi_name`

## PromQL 查询示例

### Metric 类型确认

#### app_request_count
- **类型**: Counter（累积型计数器，单调递增）
- **标签**: `service.name`（服务名称）
- **单位**: 1（无单位）
- **描述**: 应用请求计数

**代码定义位置**: `main.py` 第 433-451 行

```python
# Counter 类型，使用 Sum 聚合，is_monotonic=True
sum_data = Sum(
    data_points=[data_point],
    aggregation_temporality=1,  # CUMULATIVE
    is_monotonic=True
)
```

### PromQL 查询示例

#### 1. 按 service.name 分组显示请求速率（推荐）

查看每个服务每秒的请求数（QPS）：

```promql
# 方式1：使用 rate() 函数（推荐，显示每秒请求数）
rate(app_request_count[5m]) by (service_name)

# 方式2：如果标签名是 service.name（带点号），使用引号
rate(app_request_count[5m]) by ("service.name")
```

**说明**:
- `rate()`: 计算 Counter 的每秒增长率
- `[5m]`: 时间窗口，表示过去5分钟的数据
- `by (service_name)`: 按 `service_name` 标签分组
- **注意**: SigNoz/Prometheus 中，标签名中的点号可能被转换为下划线，请根据实际情况使用 `service_name` 或 `"service.name"`

#### 2. 按 service.name 分组显示累计请求数

查看每个服务的总请求数：

```promql
# 方式1：直接求和
sum(app_request_count) by (service_name)

# 方式2：如果标签名是 service.name
sum(app_request_count) by ("service.name")
```

#### 3. 按 service.name 分组显示请求速率总和

```promql
# 计算每个服务的总请求速率
sum(rate(app_request_count[5m])) by (service_name)

# 或者
sum(rate(app_request_count[5m])) by ("service.name")
```

#### 4. 查看特定时间范围内的请求速率

```promql
# 使用不同的时间窗口
rate(app_request_count[1m]) by (service_name)   # 1分钟窗口
rate(app_request_count[5m]) by (service_name)   # 5分钟窗口（推荐）
rate(app_request_count[15m]) by (service_name)  # 15分钟窗口
```

#### 5. 过滤特定服务

```promql
# 查看特定服务的请求速率
rate(app_request_count{service_name="ServiceTest1"}[5m])

# 或者如果标签名是 service.name
rate(app_request_count{"service.name"="ServiceTest1"}[5m])

# 使用正则表达式匹配多个服务
rate(app_request_count{service_name=~"ServiceTest.*"}[5m]) by (service_name)
```

#### 6. 计算所有服务的总请求速率

```promql
# 不分组，显示所有服务的总和
sum(rate(app_request_count[5m]))
```

#### 7. 计算每个服务的请求速率百分比

```promql
# 每个服务占总请求的百分比
rate(app_request_count[5m]) by (service_name) / sum(rate(app_request_count[5m])) * 100
```

### 在 SigNoz 中使用 PromQL

#### 方法1：使用 Metrics Explorer（推荐）

1. 进入 **Metrics Explorer**
2. 选择指标: `app_request_count`
3. 设置聚合函数: `rate`（查看速率）或 `sum`（查看累计值）
4. 设置时间窗口: `5m`（5分钟）
5. **关键步骤**: 在 **Group By** 中选择 `service.name` 或 `service_name`
6. 选择图表类型: **Line Chart**（折线图）

#### 方法2：直接输入 PromQL

1. 进入 **Metrics Explorer**
2. 切换到 **PromQL** 模式
3. 输入查询，例如：
   ```promql
   rate(app_request_count[5m]) by (service_name)
   ```
4. 执行查询

### 标签名确认方法

由于 SigNoz/Prometheus 可能将标签名中的点号转换为下划线，建议：

1. **在 SigNoz UI 中查看**:
   - 选择指标 `app_request_count`
   - 查看可用的标签列表
   - 确认是 `service.name` 还是 `service_name`

2. **使用 PromQL 查询标签**:
   ```promql
   # 查看所有标签
   label_values(app_request_count, service_name)
   # 或
   label_values(app_request_count, service.name)
   ```

3. **查看指标的所有标签**:
   ```promql
   # 查看所有时间序列
   app_request_count
   ```
   然后查看返回的标签名称

### 常用查询组合

#### 查看所有服务的请求速率对比
```promql
rate(app_request_count[5m]) by (service_name)
```

#### 查看特定几个服务的对比
```promql
rate(app_request_count{service_name=~"ServiceTest(1|2|3)"}[5m]) by (service_name)
```

#### 查看请求速率最高的前5个服务
```promql
topk(5, rate(app_request_count[5m]) by (service_name))
```

#### 查看请求速率超过阈值的服务
```promql
rate(app_request_count[5m]) by (service_name) > 100
```

### PromQL 查询注意事项

1. **Counter 类型特性**:
   - Counter 是累积值，单调递增
   - 使用 `rate()` 查看速率（每秒增长）
   - 使用 `sum()` 查看累计值
   - 不要直接使用 `app_request_count`，因为它是累积值，会一直增长

2. **时间窗口选择**:
   - 较短窗口（1m）: 更敏感，但可能有噪声
   - 中等窗口（5m）: 平衡选择（推荐）
   - 较长窗口（15m）: 更平滑，但响应较慢

3. **标签名格式**:
   - 如果 SigNoz 使用 Prometheus 格式，点号会被转换为下划线
   - 如果保留点号，需要使用引号: `"service.name"`
   - 建议先在 UI 中确认标签名

## 许可证

本项目使用与 OpenRCA 数据集相同的许可证。
